{
  "name": "jingxiao-cai-blog-eleventy-prototype",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "rm -rf dist && eleventy",
    "pagefind": "pagefind --site dist --output-subdir pagefind",
    "verify": "npm run build && npm run check:positioning && npm run pagefind",
    "check:positioning": "node scripts/check-positioning-drift.js"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "markdown-it": "^14.1.0",
    "pagefind": "^1.3.0"
  }
}
